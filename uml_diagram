@startuml
UpdateManager --> CSVManager
UpdateManager --> DBManager
UpdateManager --> Helper
DBManager <|-- DBConnection

UpdateManager : -Costructs update pipeline

CSVManager : -load CSV file
CSVManager : -provide CSV file
CSVManager : -checks CSV for correctness

DBConnection : -connection to Data Warehouse -DWH
DBManager : -checks if entries exist in DWH
DBManager : -update entries

Helper : -hashes given values after AKTIN guidelines
Helper : -return csv row in i2b2 format

@enduml

